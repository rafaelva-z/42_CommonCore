Main function

✔%c char
✔%s string
✔ %p void * (pointer) in hexadecimal format.
✔%d base 10 number.
✔%i int
✔%u Prints an unsigned decimal (base 10) number.
✔ %x Prints a number in hexadecimal (base 16) lowercase format.
✔ %X Prints a number in hexadecimal (base 16) uppercase format.
✔ %% Prints a percent sign


Other stuff

• incorrect input handling

bonus

• %(number)(flag) adds number ammount of spaces to the left of the printed
• %0(number)(flag) adds number amount of '0'
• %-(number)(flag) adds number amount of spaces - the leng, to the right of the printed 
• %# For %x, %X if its non 0, write "0x" before the hex number
• %' ' prints space if positive and - if negative (only with %d and %i)
• %+ prints + if num is positive and - if negative (only with %d)

• %.(number) shows (number) letters with %s (none if its just %.s)


check structure:

is #, +, -, ., 0, ' '
is number
is conversion

Errors:

• No conversion flag
• ' ' flag without %d or %i
• # flag without %x or %X
• invalid argument number
